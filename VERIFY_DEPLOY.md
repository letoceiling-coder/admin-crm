# Проверка обновления кода на сервере

## Что было сделано:

1. ✅ Создан тестовый файл `DEPLOY_TEST.md`
2. ✅ Закоммичен в git (коммит: `a7db182`)
3. ✅ Отправлен в репозиторий GitHub

## Текущий статус:

- **Локальный коммит:** `a7db182` - "Test: verify deploy updates code on server"
- **Удаленный коммит (до deploy):** `1ec3902` - "Deploy: 2026-01-26 20:36:42"

## Теперь выполните deploy:

```bash
cd c:\OSPanel\domains\ADMIN-CRM\crm
php artisan deploy
```

## Что должно произойти:

1. Команда обнаружит, что есть новый коммит в репозитории
2. Отправит POST запрос на сервер
3. Сервер выполнит `git pull` и получит новый коммит `a7db182`
4. В логах должно быть: "Код обновлен: 1ec3902 → a7db182"

## Как проверить на сервере:

После выполнения deploy, проверьте на сервере:

```bash
cd ~/crm.neeklo.ru/public_html

# Проверить последний коммит
git log -1

# Должен быть: a7db182 Test: verify deploy updates code on server

# Проверить наличие тестового файла
ls -la DEPLOY_TEST.md

# Должен существовать файл DEPLOY_TEST.md
```

## Ожидаемый результат deploy:

В выводе команды должно быть:
- ✅ "Код обновлен: 1ec3902 → a7db182"
- ✅ "commit_changed": true
- ✅ Git Pull: success

Если видите это - значит deploy работает правильно!
